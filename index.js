function getFirstSelector(selector) {
  var firstselect = document.querySelector(selector);
  return firstselect;
}
function nestedTarget(target) {
  var nest = document.querySelector('div.nested target');
  return nest;
}